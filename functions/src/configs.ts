import dotenv from 'dotenv';

dotenv.config();
export const { FIREBASE_HOSTING_URL } = process.env;
export const { SHOPIFY_API_KEY } = process.env;
export const { SHOPIFY_APP_NAME } = process.env;
export const { SHOPIFY_SECRET } = process.env;
